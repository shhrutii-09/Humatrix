@inherits LayoutComponentBase

<div class="auth-wrapper">
    @Body
</div>

<style>
    .auth-wrapper {
        /* This ensures the background covers the whole screen */
        min-height: 100vh;
        width: 100%;
        display: flex;
        flex-direction: column;
        background-color: #f8f9fa; /* Or whatever your theme background is */
    }

    /* We remove the .auth-card class entirely because your
           login component already has its own styling and width */
</style>
@page "/org/employees-list"
@page "/hr/employees-list"
@rendermode InteractiveServer

@using Humatrix_HRMS.DTOs
@using Humatrix_HRMS.Data
@using Humatrix_HRMS.Services
@using Microsoft.AspNetCore.Authorization
@using Microsoft.AspNetCore.Components.Authorization
@using Microsoft.AspNetCore.Identity

@attribute [Authorize(Roles = "OrgAdmin,HR")]

@inject EmployeeService EmployeeService
@inject DepartmentService DeptService
@inject UserManager<ApplicationUser> UserManager
@inject AuthenticationStateProvider AuthStateProvider
@inject NavigationManager NavManager

<h3 class="mb-3">Employee Management</h3>

<div class="mb-3 d-flex justify-content-between">
    <button class="btn btn-secondary" @onclick="GoBack">← Dashboard</button>
    <button class="btn btn-primary" @onclick="GoToAdd">+ Add Employee</button>
</div>

<div class="card p-4 shadow-sm">

    @if (!isHR)
    {
        <InputSelect TValue="Guid?"
                     Value="selectedDepartmentId"
                     ValueChanged="OnDepartmentChanged"
                     ValueExpression="() => selectedDepartmentId"
                     class="form-control mb-3">

            <option value="">-- All Departments --</option>

            @foreach (var dept in departments)
            {
                <option value="@dept.DepartmentId">@dept.Name</option>
            }
        </InputSelect>
    }

    @if (employees == null)
    {
        <p>Loading...</p>
    }
    else if (!employees.Any())
    {
        <p>No employees found</p>
    }
    else
    {
        <table class="table table-hover">
            <thead class="table-dark">
                <tr>
                    <th>Name</th>
                    <th>Email</th>
                    <th>Organization</th>
                    <th>Department</th>
<<<<<<< HEAD
                    <th>Designation</th>
=======
                    <th>Designation</th> <!-- ✅ NEW -->
                    @* <th>Designation</th> *@
>>>>>>> 78f416305aa7332ecb4231ce726efacb44858935
                    <th>Role</th>
                    <th>Status</th>
                    <th>Actions</th>
                </tr>
            </thead>

            <tbody>
                @foreach (var emp in employees)
                {
                    <tr>
                        <td>@emp.Name</td>
                        <td>@emp.Email</td>
                        <td>@emp.Organization</td>
                        <td>@emp.Department</td>
<<<<<<< HEAD
                        <td>@emp.Designation</td>

                        <td>@(emp.IsHR ? "HR" : "Employee")</td>
                        <td>@(emp.IsActive ? "Active" : "Inactive")</td>
=======
                        <td>@emp.Designation</td> <!-- ✅ NEW -->

                        @* <td>@emp.Designation</td> *@
                        <td>
                            @if (emp.IsHR)
                            {
                                <span class="badge bg-primary">HR</span>
                            }
                            else
                            {
                                <span class="badge bg-secondary">Employee</span>
                            }
                        </td>
>>>>>>> 78f416305aa7332ecb4231ce726efacb44858935

                        <td>
                            <button class="btn btn-sm btn-danger"
                                    @onclick="() => DeleteUser(emp.Email!)">
                                Delete
                            </button>
<<<<<<< HEAD
=======

                            @if (emp.IsActive)
                            {
                                <button class="btn btn-sm btn-danger me-1"
                                        @onclick="() => ToggleStatus(emp.Email, false)">
                                    Deactivate
                                </button>
                            }
                            else
                            {
                                <button class="btn btn-sm btn-success me-1"
                                        @onclick="() => ToggleStatus(emp.Email, true)">
                                    Activate
                                </button>
                            }


>>>>>>> 78f416305aa7332ecb4231ce726efacb44858935
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
</div>

@code {
    private List<EmployeeListDto>? employees;
    private List<DepartmentDto> departments = new();

    private bool isHR = false;
    private Guid? selectedDepartmentId;

    protected override async Task OnInitializedAsync()
    {
        departments = await DeptService.GetAllAsync();

        var authState = await AuthStateProvider.GetAuthenticationStateAsync();
        var user = await UserManager.GetUserAsync(authState.User!);

        if (user != null)
        {
            var roles = await UserManager.GetRolesAsync(user);
            isHR = roles.Contains("HR");
        }

        await LoadEmployees();
    }

    private async Task LoadEmployees()
    {
        employees = await EmployeeService.GetEmployeesForListAsync(selectedDepartmentId);
    }

    private async Task DeleteUser(string email)
    {
        await EmployeeService.DeleteUserAsync(email);
        await LoadEmployees();
    }

    private async Task OnDepartmentChanged(Guid? value)
    {
        selectedDepartmentId = value;
        await LoadEmployees();
    }

    private void GoToAdd()
    {
        NavManager.NavigateTo("/org/employees");
    }

    private void GoBack()
    {
        NavManager.NavigateTo("/dashboard");
    }
}

@page "/admin/create-organization"
@using Humatrix_HRMS.DTOs
@using Humatrix_HRMS.Services
@using Microsoft.AspNetCore.Authorization

@* Restrict this page to SuperAdmins only *@
@attribute [Authorize(Roles = "SuperAdmin")]
@inject OrganizationService OrgService

<h3>Create New Organization</h3>

<div class="card p-4 shadow-sm">
    <EditForm Model="orgDto" OnValidSubmit="HandleCreateOrganization" FormName="createOrgForm">
        <DataAnnotationsValidator />
        <ValidationSummary class="text-danger" />

        <div class="mb-3">
            <label class="form-label">Organization Name</label>
            <InputText @bind-Value="orgDto.Name" class="form-control" placeholder="e.g. Acme Corp" />
        </div>

        <div class="mb-3">
            <label class="form-label">Organization Email</label>
            <InputText @bind-Value="orgDto.Email" class="form-control" />
        </div>

        <div class="mb-3">
            <label class="form-label">Phone</label>
            <InputText @bind-Value="orgDto.Phone" class="form-control" />
        </div>

        <div class="mb-3">
            <label class="form-label">Address</label>
            <InputTextArea @bind-Value="orgDto.Address" class="form-control" />
        </div>

        <div class="mb-3 border-top pt-3">
            <label class="form-label text-primary">Admin User Email</label>
            <InputText @bind-Value="orgDto.AdminEmail" class="form-control" placeholder="admin@org.com" />
            <small class="text-muted">A setup link will be generated for this email.</small>
        </div>

        <button type="submit" class="btn btn-success" disabled="@isSubmitting">
            @if (isSubmitting)
            {
                <span>Creating...</span>
            }
            else
            {

                <span>Create & Generate Link</span>
            }
        </button>
    </EditForm>
</div>

@if (!string.IsNullOrEmpty(generatedLink))
{
    <div class="alert alert-info mt-4">
        <h5>Organization Created Successfully!</h5>
        <p>Copy the link below and send it to the Organization Admin:</p>
        <div class="input-group">
            <input type="text" class="form-control" value="@generatedLink" readonly />
        </div>
    </div>
}

@code {
    [SupplyParameterFromForm]
    public CreateOrganizationDto orgDto { get; set; } = new();

    private string? generatedLink;
    private bool isSubmitting = false;

    private async Task HandleCreateOrganization()
    {
        isSubmitting = true;
        try
        {
            // Calls the service method you already created [cite: 2]
            generatedLink = await OrgService.CreateOrganizationAsync(orgDto);
            // Optional: Reset form after success
            orgDto = new();
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error: {ex.Message}");
        }
        finally
        {
            isSubmitting = false;
        }
    }
}